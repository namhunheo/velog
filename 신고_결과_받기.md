## [코테] 신고 결과 받기 (해시 + 집합)


문제 : 신고 결과 받기
관련 알고리즘 : 해시(Hashmap) + 집합(Set)
https://school.programmers.co.kr/learn/courses/30/lessons/92334?language=java

###Hash : 임의 길이의 데이터를 고정된 길이의 값으로 바꾸는 것
입력: 문자열, 파일, 숫자 등 아무 데이터
출력: 항상 고정 길이의 값 (해시값)
같은 입력 → 같은 해시값
조금만 달라도 → 완전히 다른 해시값

왜 사용?

비밀번호 저장(원문 대신 해시값만 저장)
빠른 검색(해시 테이블)
데이터 무결성 확인(파일이 바뀌었는지 체크)
중요 특징
1. 되돌릴 수 없음 (해시값 -> 원본 불가)
2. 충돌이 거의 없음 (다른 입력인데 같은 해시값 나오는 경우 거의 없음)
3. 빠름

HashMap에서 hash도 데이터를 숫자로 바꾸는 함수를 사용해
key → hash 함수 → 숫자에서 O(1)로 저장 위치를 빠르게 찾는 것이 목적
HashMap : 키로 값을 찾고, 상황에 따라 값(카운트)을 늘리거나 줄일 수 있는 유연한 자료구조
사물함을 예시로
키(key) → 사물함 번호 (예: 101번)
값(value) → 사물함 안 물건 (예: 가방)

101 → 가방
102 → 책
103 → 옷
👉 102번 사물함을 열면 바로 책을 꺼낼 수 있음
(하나씩 뒤질 필요 없음)

목적
1. 빠른 매핑 -> O(1)
2. 정확함 (키 중복 불가)
3. 카운트
카운트 증가는 뭔데?
: 어떤 일이 발생할 때마다 숫자를 1씩 더하는 처리
카운트 증가 + 감소 : 장바구니 재고 관리 등
사과 5개 → 손님이 2개 구매
재고: 5 → 5-2 = 3  (감소)
새 배송 4개 → 3 + 4 = 7 (증가)
Set : 중복을 허용하지 않는 자료구조
예시) 철수, 철수, 영희 → {철수, 영희}
-> 중복 제거

특징
1. 중복 자동 제거 (add()해도 이미 있으면 무시)
2. 순서 중요 x (들어온 순서 보장 x)
3. "있는지 확인"이 빠름 (contains() -> 거의 O(1))

HashMap vs Set

HashMap: 키–값 저장 (키 중복 x, 값 중복 o) / 연결(매핑)
Set: 값만 저장(중복 x) / 존재 여부

문제



풀이
요약:
여러 사람이 다른 사람을 신고했고, k번 이상 신고당한 사람은 정지.
누군가 신고한 사람 중 실제로 정지된 사람 수를 구하기..?

먼저 생각할 것들
1) 누가 누구를 신고했는지
2) 각 사람이 몇번 신고당했는지
3) 정지된 사람을 신고한 사람에게 메일로 보냄

표 정리하면

muzi -> frodo / apeach -> frodo / frodo -> neo / muzi -> neo
신고당한 횟수
: frodo랑 neo : 2번 -> 정지

각자 받는 메일

muzi = 2
apeach = 1
frodo = 1
neo = 0
--> 사람 이름 기준으로 정보 저장? = 해시맵
--> 한 사람이 여러 명 신고 가능한데, 중복은 1번만 인정이면 Set

코드 구현
누가 누구 신고했는지 저장
Map<String, Set<String>> reportedBy = new HashMap<>();
key: 신고당한 사람
value: 그 사람을 신고한 사람들 (중복 제거 → Set)
EX) "frodo" → {muzi, apeach}
중복 신고는 1번만 인정
reportedBy.get("frodo").add("muzi");
k번 이상 신고당한 사람만 정지
if (reportedBy.get(user).size() >= k) {
    // 정지된 사람
}
최종

import java.util.*;

class Solution {
    public int[] solution(String[] id_list, String[] report, int k) {

        // 1. 신고당한 사람 → 신고한 사람들
        Map<String, Set<String>> reportedBy = new HashMap<>();

        // 모든 유저를 미리 Map에 넣어둔다
        for (String id : id_list) {
            reportedBy.put(id, new HashSet<>());
        }

        // 2. 신고 기록 처리
        for (String r : report) {
            // "muzi frodo" → ["muzi", "frodo"]
            String[] parts = r.split(" ");
            String from = parts[0]; // 신고한 사람
            String to = parts[1];   // 신고당한 사람

            // Set이라서 중복 신고 자동 제거
            reportedBy.get(to).add(from);
        }

        // 3. 정지된 사람 찾기
        Set<String> banned = new HashSet<>();

        for (String user : reportedBy.keySet()) {
            // 신고한 사람 수가 k 이상이면 정지
            if (reportedBy.get(user).size() >= k) {
                banned.add(user);
            }
        }

        // 4. 결과 배열 (id_list 순서대로)
        int[] answer = new int[id_list.length];

        // id_list 순서 보존을 위해 인덱스 사용
        for (int i = 0; i < id_list.length; i++) {
            String me = id_list[i];

            // 내가 누구를 신고했는지 다시 확인
            for (String bannedUser : banned) {
                if (reportedBy.get(bannedUser).contains(me)) {
                    answer[i]++;
                }
            }
        }

        return answer;
    }
}
HashSet은 중복 신고 막는 용도
HashMap은 누구의 Set인지 연결하는 용도
꼬꼬무
Set vs List

List = [A, A, B] -> 중복 허용
Set = {A, B} -> 중복 불가

HashSet vs TreeSet

HashSet = 빠름, add / contains → O(1)
TreeSet = 정렬됨, add / contains → O(log N)

예시) 입력: 3, 1, 2
HashSet → {3, 1, 2} (순서 보장 X, 빠름) -> 중복 제거 + 속도 중요
TreeSet → {1, 2, 3} (자동 정렬, 느림) -> 중복 제거 + 정렬 필요